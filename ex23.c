#include <stdio.h>

int main(){
    double n;
    double b;
    double a;
    double media;
    b = 0;

    printf("inserire un numero\n");
    scanf("%lf" , &n);

    while(n>=0||b>)
    { 
        b = b + 1;
        n = n + a;
        media = n / b;
        printf("la media è: %lf\n", media);
        printf("inserire un numero\n");
        scanf("%lf" , &a);
    }
    return(0);
}